apiVersion: v1
kind: Secret
metadata:
  name: todo-api-secret
  namespace: todo-api
  labels:
    app: todo-api
type: Opaque
stringData:
  # IMPORTANT: Replace these values with actual secrets before deploying
  # In production, use GCP Secret Manager or external-secrets operator
  DATABASE_USER: "todo_user"
  DATABASE_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  SECRET_KEY: "REPLACE_WITH_SECURE_SECRET_KEY"
